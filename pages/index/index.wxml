<!--index.wxml-->
<view class="c">
  <!--longitude为经度值,latitude为纬度值,scale为缩放范围（1-18）-->
  <map 
    class="a" 
    longitude="{{longitude}}" 
    latitude="{{latitude}}"
    scale="{{a}}"
    ></map>
</view>
